apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: calculator
    component: backend
spec:
  type: ClusterIP
  selector:
    app: calculator
    component: backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
