apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: calculator
    component: frontend
spec:
  type: LoadBalancer
  selector:
    app: calculator
    component: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
