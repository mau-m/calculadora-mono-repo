apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base

configMapGenerator:
  - name: backend-config
    literals:
      - ENVIRONMENT=dev
      - ALLOWED_ORIGINS=*
      - LOG_LEVEL=debug
